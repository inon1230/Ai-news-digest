# 🚀 מדריך התקנה - AI News Digest

## מה זה עושה?
מערכת אוטומטית שרצה כל בוקר, אוספת חדשות AI מ-8 אתרים מובילים, מסננת את המיותר, ומספקת לך סיכום תמציתי.

---

## 📋 דברים שתצטרך לפני שמתחילים

1. **חשבון GitHub** (חינמי) - https://github.com/signup
2. **API Key של Claude** (חינמי עד $5/חודש) - https://console.anthropic.com/
3. **10 דקות של הזמן שלך**

---

## 🎯 שלב 1: קבלת API Key מ-Anthropic

1. היכנס ל: https://console.anthropic.com/
2. לחץ על **"Get API Keys"** בתפריט העליון
3. לחץ על **"Create Key"**
4. תן לו שם (למשל: "News Digest")
5. **העתק את ה-Key** - תצטרך אותו מיד! (הוא לא יוצג שוב)
6. שמור אותו במקום זמני

💡 **חשוב**: ה-API Key מתחיל ב-`sk-ant-...`

---

## 🎯 שלב 2: יצירת Repository ב-GitHub

1. היכנס ל-GitHub שלך
2. לחץ על הכפתור **"New"** (ליד Repositories)
3. תן שם ל-Repository: `ai-news-digest`
4. סמן את **"Public"** או **"Private"** (לא משנה)
5. **אל** תסמן את "Add a README file"
6. לחץ **"Create repository"**

---

## 🎯 שלב 3: העלאת הקבצים ל-GitHub

יש לך 2 אפשרויות:

### אפשרות א': דרך הממשק של GitHub (הכי קל!)

1. בדף ה-Repository החדש שנפתח, לחץ על **"uploading an existing file"**
2. גרור את כל הקבצים שיצרתי לך (או לחץ "choose your files")
3. תוודא שיש לך:
   - `news_digest.py`
   - `requirements.txt`
   - תיקייה `.github/workflows/` עם הקובץ `daily-digest.yml` בפנים
4. לחץ **"Commit changes"**

### אפשרות ב': דרך Git (אם אתה מכיר)

```bash
cd ai-news-digest
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/ai-news-digest.git
git push -u origin main
```

---

## 🎯 שלב 4: הוספת ה-API Key ל-GitHub (סודות)

זה השלב הכי חשוב! 🔐

1. בדף ה-Repository שלך, לחץ על **"Settings"** (למעלה)
2. בתפריט צד שמאל, לחץ **"Secrets and variables"** → **"Actions"**
3. לחץ על הכפתור הירוק **"New repository secret"**
4. **Name**: הקלד בדיוק `ANTHROPIC_API_KEY`
5. **Secret**: הדבק את ה-API Key ששמרת קודם
6. לחץ **"Add secret"**

✅ אם עשית הכל נכון, תראה את השם `ANTHROPIC_API_KEY` ברשימה

---

## 🎯 שלב 5: הפעלת האוטומציה

### בדיקה ראשונה (הרצה ידנית):

1. לך ל-**"Actions"** בתפריט העליון של ה-Repository
2. תראה workflow בשם **"AI News Digest - Daily Summary"**
3. לחץ עליו
4. לחץ על **"Run workflow"** → **"Run workflow"** (כפתור ירוק)
5. תראה שהוא מתחיל לרוץ - זה ייקח כ-30 שניות

### צפייה בתוצאות:

1. לאחר שהריצה הסתיימה (יהיה סימן ✅ ירוק)
2. לחץ על שם הריצה
3. גלול למטה ל-**"Artifacts"**
4. תראה קובץ להורדה בשם `ai-news-digest-1`
5. הורד אותו ותיהנה מהסיכום שלך! 🎉

---

## ⏰ לוח זמנים אוטומטי

המערכת מוגדרת לרוץ **כל יום בשעה 08:00 UTC**:
- **בחורף** (אוקטובר-מרץ): זה 10:00 בוקר בישראל
- **בקיץ** (אפריל-ספטמבר): זה 11:00 בוקר בישראל

### רוצה לשנות את השעה?

1. פתח את הקובץ `.github/workflows/daily-digest.yml`
2. מצא את השורה: `- cron: '0 8 * * *'`
3. שנה את המספר השני (8) לשעה אחרת:
   - `'0 6 * * *'` = 08:00 בוקר בישראל (חורף)
   - `'0 10 * * *'` = 12:00 צהריים בישראל (חורף)
   - `'0 18 * * *'` = 20:00 ערב בישראל (חורף)

---

## 🎨 התאמה אישית

### שינוי רשימת האתרים:

פתח את `news_digest.py` ומצא את:
```python
NEWS_SOURCES = {
    "שם המקור": "כתובת RSS",
    ...
}
```

**כיצד למצוא RSS Feed של אתר?**
- רוב אתרי החדשות יש להם `/feed` או `/rss` בסוף ה-URL
- חפש באתר "RSS" או סמל 📡
- או השתמש ב-Google: `"שם האתר" RSS feed`

### שינוי כמות השעות לאחור:

מצא את: `HOURS_BACK = 24` ושנה למספר רצוי

---

## 📊 איך לקבל את הסיכום?

כרגע הסיכום נשמר כ-Artifact ב-GitHub. יש לך כמה אפשרויות:

### אפשרות 1: הורדה ידנית מ-GitHub
- כל יום נכנס ל-Actions ומוריד את הקובץ

### אפשרות 2: שליחה לאימייל (צריך הוספת קוד)
- אפשר להשתמש ב-GitHub Action שישלח את הקובץ למייל

### אפשרות 3: שמירה ב-Google Drive
- אפשר להשתמש ב-API של Google Drive

### אפשרות 4: שליחה לטלגרם
- הכי קל! צריך רק Telegram Bot Token

---

## 🆘 פתרון בעיות נפוצות

### ❌ "Error: ANTHROPIC_API_KEY not found"
- בדוק שהוספת את ה-Secret נכון ב-Settings → Secrets
- וודא שהשם הוא בדיוק `ANTHROPIC_API_KEY` (עם קו תחתון)

### ❌ "Invalid API Key"
- ה-API Key לא נכון או שפג תוקפו
- צור Key חדש ב-Anthropic Console

### ❌ לא רואה Artifacts
- וודא שהריצה הסתיימה בהצלחה (✅ ירוק)
- לפעמים לוקח 30 שניות אחרי שהריצה הסתיימה

### ❌ לא נמצאו כתבות חדשות
- אולי באמת לא היו חדשות ב-24 שעות האחרונות
- נסה להגדיל את `HOURS_BACK` ל-48

---

## 💰 עלויות

- **GitHub Actions**: חינמי לחלוטין (2000 דקות/חודש)
- **Claude API**: 
  - קבלת חדשות: חינם (RSS)
  - סיכום עם Claude: ~$0.01-0.03 ליום
  - **סה"כ לחודש**: ~$0.30-0.90 (פחות משקל!)

---

## 🚀 שדרוגים עתידיים

כשתהיה מוכן, אפשר להוסיף:
1. ✉️ שליחה אוטומטית למייל או טלגרם
2. 📊 יצירת PDF מעוצב במקום TXT
3. 📈 גרפים וסטטיסטיקות
4. 🎯 סינון לפי נושאים ספציפיים (LLMs, Computer Vision, וכו')
5. 🌐 תרגום אוטומטי לעברית

---

## 📞 זקוק לעזרה?

אם משהו לא עובד, בדוק:
1. ה-Actions Log ב-GitHub (יש שם הודעות שגיאה מפורטות)
2. שה-API Key נכון וה-Secret קיים
3. שכל הקבצים הועלו במבנה הנכון

בהצלחה! 🎉
